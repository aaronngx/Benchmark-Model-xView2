# Optional: dataset root override (CLI uses --dataset_root instead)
# DATASET_ROOT=data/raw/test_images_labels_targets

# Track 4 — GitHub Models (required for VLM benchmarks)
# Create a PAT at: Settings → Developer settings → Personal access tokens → Fine-grained
# Required permission: Models (read)  — or a classic PAT with the "models" scope
# Endpoint: https://models.github.ai/inference/chat/completions
# GITHUB_TOKEN=ghp_...
