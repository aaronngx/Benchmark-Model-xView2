# Optional: dataset root override (CLI can use --dataset_root instead)
# DATASET_ROOT=data/raw/test_images_labels_targets

# Optional: VLM / API keys (for Track 1/3 when using cloud VLMs)
# OPENAI_API_KEY=
# GOOGLE_API_KEY=
# ANTHROPIC_API_KEY=
