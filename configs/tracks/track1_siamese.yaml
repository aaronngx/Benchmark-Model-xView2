# Track 1 — Siamese classifier (dual-stream encoder)
# Ref §2.1: "Siamese classifier: encode pre and post separately → fuse → 4-class"
model_type:       siamese
model_ckpt:       models/siamese/best.pt
device:           cuda
input_size:       128
inference_batch:  64
crops_dir:        data/processed/crops_oracle
