# Track 4 — VLM benchmark (grounded mode: image + geometry tool context)
# Uses GitHub Models API. Set GITHUB_TOKEN in .env.
# Model IDs: run list_github_models() to see what's available for your account.
model:             openai/gpt-4o
grounded:          true            # passes change_score/pct_changed/ssim to VLM as context
crops_dir:         data/processed/crops_oracle
vlm_input_size:    512
max_crops:         200
